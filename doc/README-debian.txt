For building the frontpanel machines on some Debian Linux for X11 you need:

sudo apt install libjpeg9-dev libgl1-mesa-dev libglu1-mesa-dev

and for SDL2:

sudo apt install libsdl2-dev libsdl2-image-dev
